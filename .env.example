# ==========================================
# KIZUNAFIT - Root Docker Compose Environment Template
# ==========================================

# Docker Compose Host Publishing Port for Redis
# 63790 is the currently verified project development host port and is outside the currently observed Windows excluded ranges.
# The container internal port is always 6379.
REDIS_HOST_PORT=63790
