export { registerIdentityModule } from './module';

// The Identity Module Public Facade.
// Only export public contracts, domain events, and the registration function.
// NEVER export use cases, controllers, infrastructure repositories, or Mongoose schemas.
